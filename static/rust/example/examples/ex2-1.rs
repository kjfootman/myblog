fn main() {

//-------------------- i32 정수형 변수 예제 --------------------//
    let a: i32 = 6;
    let b: i32 = 4;
    // 자료형이 서로 다를 경우 연산을 할 수 없다.
    // let b: i64 = 4;
    let c = a + b;

    println!("{a} + {b} = {c}");

//-------------------- i64 정수형 변수 예제 --------------------//
    let a: i64 = 6;
    let b: i64 = 4;
    // 자료형이 서로 다를 경우 연산을 할 수 없다.
    // let b: i32 = 4;
    let c = a + b;

    println!("{a} + {b} = {c}");
}